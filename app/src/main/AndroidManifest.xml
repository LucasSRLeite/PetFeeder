<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lucasleite.petfeeder" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="pf.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pf.CreatePetActivity"
            android:label="@string/title_activity_create_pet" >
        </activity>
        <activity
            android:name="pf.MyPetsActivity"
            android:label="@string/title_activity_my_pets" >
        </activity>
    </application>

</manifest>
